#DISABLE EUREKA
eureka.client.enabled=false
#################################################3
slack.slashCommandToken = slashCommandToken

#Eureka client
spring.application.name=gamification-slackbot
server.port=0
eureka.client.service-url.defaultZone=${EUREKA_URI:http://localhost:8761/eureka}